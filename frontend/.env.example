VITE_API_BASE=/api/ml
# Leave unset in local dev (Vite proxies /api/auth and /api/patient → Node on :5000).
# Set the full URL only when the built app is hosted without a reverse proxy to the API.
# VITE_NODE_API_BASE=https://your-api.example.com/api
